# .env.test — 테스트 환경 전용

NODE_ENV=test
PORT=3001

# ★ 테스트 전용 DB (프로덕션 DB와 분리!)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=feature_flag_test
DB_USER=root
DB_PASSWORD=rootpassword

# Redis (같은 인스턴스를 사용하되, flushdb로 초기화하므로 별도 DB 번호 사용)
REDIS_HOST=localhost
REDIS_PORT=6379

JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=86400